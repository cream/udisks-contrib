from distutils.core import setup

setup(
    name = 'UDisks Contrib Libraries',
    version = '0.1',
    package_dir = {'cream.contrib.udisks': 'udisks'},
    packages = ['cream.contrib.udisks'],
    )
